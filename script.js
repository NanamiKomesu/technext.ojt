console.log(test);

$(document).on("ready",function(){
    $(".pcslider").slick({
       autoplay:true,
       autoplaySpeed:3000,
    });
});

$(document).on("ready",function(){
    $(".spslider").slick({
       autoplay:true,
       autoplaySpeed:3000,
    });
});